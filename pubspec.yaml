name: guaverroots
description: Offline-first crop health app for smallholder farmers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Localization
  flutter_localizations:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  
  # State Management
  provider: ^6.1.1
  
  # Camera & Image
  camera: ^0.10.5+5
  image_picker: ^1.0.5
  image: ^4.1.7
  
  # HTTP & API
  dio: ^5.4.0
  connectivity_plus: ^5.0.2
  
  # Local Storage (Offline-first)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  
  # Permissions
  permission_handler: ^11.1.0
  geolocator: ^10.1.0
  
  # AI
  tflite_flutter: ^0.12.1

  # Utils
  intl: ^0.20.2

  # Notifications
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.3

  # Voice
  flutter_tts: ^3.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    - assets/models/
    - assets/labels/
